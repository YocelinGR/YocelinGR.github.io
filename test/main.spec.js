var chai = require('chai');
var assert = chai.assert;
var describe = chai.describe;
var it = chai.it;

describe('main', () =>{
	it('should return -1 when the value is not present', function () {
		assert.isFunction(aboutLeft, 'great, is function');
	});
	it('should return -1 when the value is not present', function () {
		assert.isFunction(aboutCenter, 'great, is function');
	});
	it('should return -1 when the value is not present', function () {
		assert.isFunction(aboutR, 'great, is function');
	});
});
